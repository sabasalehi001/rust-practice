# JSON Inspector

A simple command-line tool to validate JSON files.

## Usage

```bash
json_inspector <path_to_json_file>
```

## Features

*   Validates JSON syntax.
*   Provides error messages for invalid JSON.

## Installation

```bash
cargo build --release
```